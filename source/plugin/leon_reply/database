CREATE TABLE pre_forum_reply_me(
    uid INT UNSIGNED NOT NULL PRIMARY KEY COMMENT '用户id'，
    is_reply TINYINT NOT NULL DEFAULT 0 COMMENT '1 开启 2 关闭',
    PRIMARY KEY (uid)
) CHARSET=UTF8 ENGINE=MYISAM